# paas-custom

![Version: 1.0.0](https://img.shields.io/badge/Version-1.0.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.29.2](https://img.shields.io/badge/AppVersion-1.29.2-informational?style=flat-square)

PaaS chart for arbitrary customer containers (custom apps) — wraps the generic webapp sub-chart under the `custom` alias, no template fork (ADR-0004 paas-console)

## Requirements

| Repository | Name | Version |
|------------|------|---------|
| https://kuzzleio.github.io/helm-charts | custom(webapp) | 1.5.0 |

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| custom.extraEntrypoints | list | `[]` | Points d'entrée additionnels (un par protocole exposé par le conteneur) |
| custom.image.name | string | `"nginx"` | Référentiel de l'image conteneur (placeholder — la console pose l'image projet) |
| custom.image.pullPolicy | string | `"Always"` | Politique de pull de l'image |
| custom.image.tag | string | `"latest"` | Tag de l'image (version) |
| custom.probes.liveness.enable | bool | `false` | Active/désactive la livenessProbe (off par défaut : conteneur arbitraire) |
| custom.probes.readiness.enable | bool | `false` | Active/désactive la readinessProbe (off par défaut : conteneur arbitraire) |
| custom.replicaCount | int | `1` | Nombre de réplicas du Deployment |
| custom.service.port | int | `80` | Port du Service (la console pose le customPort de l'app) |
| custom.service.targetPort | int | `80` | Port du conteneur ciblé par le Service |
| custom.updateStrategy | string | `"RollingUpdate"` | Stratégie d'update du Deployment (RollingUpdate|Recreate) |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
