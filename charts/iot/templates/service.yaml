apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot.fullname" . }}
  labels:
    {{- include "iot.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "iot.selectorLabels" . | nindent 4 }}

