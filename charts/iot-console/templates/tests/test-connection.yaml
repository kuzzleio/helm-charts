apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iot-console.fullname" . }}-test-connection"
  labels:
{{ include "iot-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "iot-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
