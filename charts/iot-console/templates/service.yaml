apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot-console.fullname" . }}
  labels:
    {{- include "iot-console.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "iot-console.selectorLabels" . | nindent 4 }}

